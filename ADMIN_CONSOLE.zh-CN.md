# 管理台

**简体中文** · [English](ADMIN_CONSOLE.md)

管理台位于 `/admin`，供单个预先配置的管理员使用，包含总览、任务与用户、Agent 与厂商、预算、反馈与自进化、存储管理和操作记录。使用系统字体，支持桌面和手机。通用代码开源；真实账号绑定、密钥、部署地址和运行数据留在私有配置与持久化存储中。

## 登录与初始化

未设置 `WHAT_THE_REPO_ADMIN_GITHUB_ID` 时管理接口不可用。设置的是 GitHub 不变的数字账号 ID，不能使用昵称；普通 GitHub 登录、访客会话和第一个访问者都没有管理权限。

生产模式要求 PostgreSQL、HTTPS 和至少 32 字符的 `WHAT_THE_REPO_KEY_ENCRYPTION_SECRET`，也支持其 `_FILE` 形式。API、分析 Worker 和自进化 Worker 使用同一个加密密钥。密钥只能保存到私有配置或 Secret，不能提交。启用前应用数据库迁移，包括 `0020_admin_console.sql`。

1. 在私有位置生成首次绑定凭据，例如 `node scripts/create-admin-bootstrap.mjs .secrets/admin-bootstrap.txt`。脚本拒绝覆盖文件，仅输出 SHA-256 摘要。
2. 将摘要设置为 `WHAT_THE_REPO_ADMIN_BOOTSTRAP_SHA256`，私下保存原始凭据。
3. 用指定 GitHub 账号登录，输入初始化凭据，用 Authenticator 扫码，再输入验证码确认绑定。
4. 安全保存仅显示一次的 10 个恢复码。首次绑定完成后，初始化凭据不能用于重新绑定。

每次登录完成 GitHub 与 TOTP 两步验证，随后正常操作使用管理会话，不会逐次要求验证码。会话最长 8 小时，30 分钟没有授权请求后失效；注销立即撤销当前会话。每个管理接口在服务端鉴权，写操作还检查来源、专用请求头与 CSRF Token。

TOTP 使用 SHA-1、6 位数字、30 秒步长，允许前后一个时间步。已接受的时间步不能重放；连续 5 次失败锁定 15 分钟，状态跨进程与重启保存。种子采用 AES-256-GCM 加密，恢复码只保存摘要。验证码、种子、Key、恢复码和请求正文不进入操作日志。实现包含 [RFC 6238](https://www.rfc-editor.org/info/rfc6238/) 测试向量；更换流程参考 [OWASP MFA 指引](https://cheatsheetseries.owasp.org/cheatsheets/Multifactor_Authentication_Cheat_Sheet.html)。

更换验证器时先注销并重新进行 GitHub 登录，在二步验证页输入当前验证码，选择更换；丢失验证器时输入一次性恢复码。两种流程都会撤销旧会话，只有确认新验证器后才产生新会话与恢复码。恢复码不会直接授予管理权限。验证器和恢复码同时丢失时需要维护者通过私有运维恢复存储，不提供公开重置或“第一个账号接管”入口。

## 独立管理域名

同源开发可直接访问 `/admin`。使用单独 HTTPS 域名时，设置 `WHAT_THE_REPO_ADMIN_WEB_URL=https://admin.example.org`，将该来源的 `/api` 转发到 API，将页面路径转发到 Web。GitHub 边界网关设置 `GITHUB_GATEWAY_ADMIN_CALLBACK_URL=https://admin.example.org/api/auth/github/callback`；签名 OAuth 请求携带固定管理受众，回调地址来自配置。普通用户继续使用原回调。Cookie 不设置共享父域。此功能不自动修改 DNS、证书、Ingress 或生产配置。

部署接入说明：k3s 的可选配置入口见 `.env.k3s.example`，网关入口见 `.env.github-gateway.example`。自进化 Secret 与 API/分析 Worker 必须包含同一 `key-encryption-secret`；仅更新 Worker 镜像而未更新 Secret 挂载，无法使用管理台保存的加密厂商配置。公开模板的静态检查不能代替线上安装与联通验证。

## 预算与用量

| 预算 | 初始程序配置 | 适用范围 |
| --- | --- | --- |
| 平台每日仓库分析 | 5 USD | 共享仓库分析、语义配套 Agent、语言覆盖 |
| 平台每日免费聊天 | 5 USD | 免费聊天及学习路线、引用检查、记忆等配套 Agent |
| 自进化单任务 | 1 USD | 每个反馈分析任务或候选生成任务的全部调用 |
| 自进化每日总额 | 5 USD | 平台反馈分析与自进化任务合计 |

金额通过管理台调整。`null` 是真正的不设限，`0` 禁止新增有费用调用；单任务与每日限制独立检查。不再读取旧的每身份每日金额、混合平台每日金额和 `WHAT_THE_REPO_EVOLUTION_MAX_COST_USD` 限额。初期没有个人金额额度或个人额度面板。

用户自带 Key 的聊天及其配套 Agent 使用该 Key，不扣平台免费额度，也不受本站每日模型金额限制。公开仓库的共享分析仍由平台承担费用，与触发者身份无关。模型验证入口按用户自带 Key 记录。反馈分析属于平台自进化业务。各入口记录业务、费用承担方、Agent 角色、连接、任务与配置版本；无法可靠分类的旧行标为 `historical_unclassified`。

有限预算使用 PostgreSQL 原子预留，模型调用在数据库事务外执行；已知用量结算释放多余预留，未知用量、取消后缺失终态和中断调用保留预留。重复回调不会重复扣款或退款。金额是程序估算，不是厂商账单；模型目录缺少可信价格时不把它当成免费，在有限平台预算下拒绝调用，在不设限或自带 Key 场景保留未知标记。预留采用保守模型上限，剩余预算不足以覆盖新请求时可能提前拒绝。

每日预算按**北京时间自然日**重置。全站分析与免费聊天额度耗尽分别返回 `site_analysis_budget_exhausted` 和 `site_chat_budget_exhausted`，提示“请明天再试”。金额设置为 0 使用 `site_budget_disabled`；上游余额不足、频率限制、空间不足有独立原因。HTTP、聊天流和任务查询复用原有错误展示，不把业务额度问题作为网络断线重试。已有结果仍可读取，自进化停止不会停用全站。

并发、调用频率、队列、任务步骤、时间和 Token 等资源保护继续生效，部署参数只能在管理台查看。预算页显示已知费用、预留、剩余、北京时间重置时间与未知用量；自进化单任务累计跨天保留。

## 厂商配置、审核和观测

反馈列表显示 GitHub 用户名和显示名称，数字身份保留作辅助定位。用户与访客列表使用服务端分页，每页 25 条。
管理台的 JSON 和代码详情使用语法高亮；候选差异通过弹窗展示统一 Git diff，包含新增/删除底色、原/新行号和代码颜色，支持 Esc 关闭。

模型配置只管理平台连接：免费聊天、仓库分析、反馈分析、自进化及仓库分析配套角色。学习路线、理解检验、引用检查和记忆维护始终跟随当前聊天选择（用户 Key 或免费聊天模型），不再提供单独管理选项，也不采用旧部署文件里的聊天配套角色覆盖。

连接可由多个 Agent 复用；Key 只显示脱敏尾部。修改目标地址需要重新填写 Key，避免把旧凭据发送给新地址。连接验证仅查询模型目录，不触发付费生成；目录验证不等于付费推理已验收。配置保存形成不可变版本，新建分析和自进化任务固定版本；聊天及配套 Agent 在每轮开始时取同一版本。版本 0 表示部署默认配置，变更部署默认参数仍需要按部署流程排空旧任务。

反馈页展示赞踩、反馈分析、来源请求、候选差异、检查评测、用量、审核和版本记录。“批准并发布”通过持久化命令交给已有自进化 Worker 执行，复用原审核、发布与回滚校验。中断中的发布命令不会自动重放；需先核对已有版本与任务账本后处理不确定状态。配置、预算、审核、清理和认证结果写入管理操作记录。

在线定义为**近 90 秒有前台心跳的身份**。前台每 25 秒发一次；相同账号或访客浏览器身份的多标签页在服务端去重。手机后台不再续期，断线后自动过期。访客数是浏览器身份的近似值。原有请求、模型、队列和数据库指标继续使用；服务采集超过 45 秒标为过期，未接入或未知的指标不显示为正常 0。Prometheus/Grafana 示例仍可独立使用。

## 存储

总览增加当前 GitHub 账号/访客身份构成图，以及默认按小时展示的平均在线和峰值图；可切换近一小时分钟趋势。后台每分钟只保存一条数量汇总，按已采样分钟计算平均值，缺失时段不补零，当前小时与不足 60 分钟的覆盖情况明确显示。采集超过 90 秒标为过期。汇总保留 7 天，图表展示近 24 小时，不记录个人浏览轨迹。需要应用 `0021_admin_audience_samples.sql`，运行账号须具备该表读写权限。

容量策略输入和显示统一使用 GB（1 GB = 1,000,000,000 字节），接口及存储仍用字节；原值往返保存不会被显示精度取整。COS 单价保留 USD/GiB/月计费口径，并标注与 GB 的换算。

不再限制每用户 4 GiB。访客生命周期仍独立按时间执行：无项目闲置 7 天删除；有项目闲置 30 天软删除，7 天恢复窗口后清理身份数据。

快照容量回收只在容量偏低时扫描已过保留窗口、无有效引用的候选，由管理员确认删除。删除事务重新检查项目绑定、仓库头、运行中任务与快照状态；任何执行中或排队任务存在时保守拒绝快照回收。用户主动删除、临时文件清理与安全有效期不依赖低容量条件。

主机按实际文件系统空间管理，默认系统/数据库保留 2 GiB，每个排队或执行任务预留 1 GiB，8 GiB 预警，4 GiB 恢复接收阈值。管理台可调整；`WHAT_THE_REPO_STORAGE_VOLUME_PATHS` 用分号列出其他实际挂载路径。未知容量或低于保留线时拒收新分析与语言处理，读取、管理和清理入口保留。

COS 根据分页对象清单统计实际字节，并使用配置容量/月费用预算，不显示虚构的磁盘剩余比例。目录采集 5 分钟后视为过期，配置有限 COS 预算时未知用量会阻止新增处理；管理台可刷新。COS 月费用为配置单价乘存储量的估计，不包含请求/流量账单。COS 删除不释放主机磁盘，数据库行删除也不承诺立即缩小数据库文件。候选回收量按去重对象大小计算，无法确定时显示未知。

统计范围为配置的 COS 前缀内已完成上传的对象，包含保留的历史版本。需要版本状态及对象版本列表读取权限；无法采集时显示未知。管理员确认容量回收会永久删除该无引用快照对象的所有版本及删除标记，普通对象删除保持原行为。删除期间保持引用/任务锁，对象失败会明确报错并保留候选以便重试。

生产采集由 API 后台每 2 分钟执行，关闭管理台不会停止；失败保留原采集时间，5 分钟过期后显示未知。产品 CAM 策略需要 `GetBucketVersioning` 和限于产品前缀的 `GetBucket`、`GetBucketObjectVersions` 读取权限，示例见 `infra/tencent/cam-product-objects-policy.json`。无需改变桶版本控制设置或取得备份对象权限。

`cos:prefix` 条件中的斜杠按腾讯云规则写成 `%2F`；前缀后的通配符允许在该前缀内部按对象读取版本列表，不授权其他目录。参见[腾讯云条件键说明](https://cloud.tencent.com/document/product/436/71307)。

## 本机验证

Windows 持续开发：在仓库根目录运行 `powershell -ExecutionPolicy Bypass -File scripts/start-admin-dev.ps1`。
启动器复用本机 15432 的 PostgreSQL，创建独立的 `wtr_admin_preview_dev` 数据库；不读取生产连接、模型 Key 或 COS 配置。
管理台地址为 `http://127.0.0.1:5391/admin`，数据控制页为 `http://127.0.0.1:8491/`。
前端使用同一份 React 源码热更新，服务端源码变化自动重启。数据控制页提供本机测试账号验证码及五种人数场景：完整 24 小时、低人数、采集缺口、空历史、采集过期。
模拟身份、反馈、候选和审核历史均保存在本机；切换场景只替换该开发库的模拟人数数据，返回管理台点击刷新即可查看。
开发候选不会执行真实发布；线上采集、真实模型及发布结果需另行验收。
测试凭据、日志和持久数据在忽略目录 `.local/admin-preview-dev/`，重启保留管理配置及未过期的会话。右下角有数据控制入口，无需绑定本人的 Authenticator。
公网更新需要另行部署；本机热更新不会修改服务器，Git 推送本身也不等于部署。

小时和分钟趋势均使用逐点直线连接，不做曲线平滑。缺失小时或分钟保留断点；选中时才显示参考线和标记点，低人数及孤立数据点仍可查看。

三个包分别运行构建和测试；`server` 的普通测试包含文件模式隔离认证，PostgreSQL 条件测试默认跳过。根目录 `node scripts/test-admin-postgres.mjs` 从忽略的本机环境文件读取开发数据库连接，只接受回环地址和 15432 端口，创建独立 `wtr_admin_test_*` 数据库，验证后只删除该测试库。

无真实 OAuth、模型或生产数据的预览：在 `server` 运行 `node --import tsx src/smoke/admin-preview.ts`；在 `web` 设置 `WHAT_THE_REPO_BACKEND_URL=http://127.0.0.1:8390` 后运行 `npm run dev -- --port 5390`，访问 `http://127.0.0.1:5390/admin`。模拟网关仅监听回环 8490，使用隔离测试 ID；仍通过实际签名 GitHub 回调、初始化凭据与 TOTP 绑定流程。初始化凭据保存在该次 `.local/admin-preview-*` 目录，不会打印。禁止在生产模式启动预览入口。

根目录 `node scripts/smoke-admin-preview.mjs <该次预览目录>` 使用模拟身份完成真实绑定并检查桌面/手机操作，保存仅属于测试身份的浏览器会话和截图。测试优先使用模拟模型，真实收费验证和线上验收需要另外执行；本机通过不代表线上通过。

通过后运行 `node scripts/open-admin-preview.mjs <该次预览目录>`，在 Chrome 打开使用该隔离会话的可操作预览。

预览包含隔离的示例任务、费用和候选，配置与预算可以操作；未连接实际分析或自进化 Worker，因此示例候选不能发布。审核、发布和回滚的服务端流程由独立测试验证。

管理台的“分析任务”按仓库汇总当前或最近一次分析，不再另列对话项目标题；参与者来自本次共享分析批次，后续复用者不计入。用户与访客在线优先，在线标准为近 90 秒前台心跳。用户和仓库列表均支持分页与指定页码跳转。仓库默认显示两位用户，超过两位可通过弹窗查看全部。管理台文字使用系统字体，代码使用等宽字体。

存储页提供已分析仓库列表：包括全部保存版本、COS 对象及保留版本用量、服务器快照/源码文件、相关数据库记录与索引、去重使用者及可确认的最后对话时间（以消息中记录的分析版本为准，旧数据缺失不推算）。数据库共享页面按记录数量分摊估算，不能作为立即可回收磁盘量；保留的对话记录不会随分析资料清理而删除。未采集、过期的对象用量保持未知。

管理员可预览影响并输入仓库名确认清理分析资料。执行前重新检查版本、引用及活动任务；活跃对话通过数据库共享锁与清理互斥，存在排队或执行中的分析时拒绝清理。先持久化资料下架状态，再删除对象和查询索引；部分删除失败仍保持不可复用状态，列表可重试，不恢复成看似完好的分析结果。此入口与低容量自动列出无引用旧快照的机制分开，普通访客生命周期规则不变。
